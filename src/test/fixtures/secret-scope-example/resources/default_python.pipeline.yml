resources:
  pipelines:
    default_python_pipeline:
      name: default_python_pipeline
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true

      libraries:
        - glob:
            include: ../src/transformations/**
